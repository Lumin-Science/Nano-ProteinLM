# Local roots only. Copy to .env and edit these two paths.
# Prepared training data: $DATA_ROOT/training
# Frozen contact data and evaluator: $DATA_ROOT/evaluation/{contact,source}
DATA_ROOT="${DATA_ROOT:-$PWD/data}"
OUTPUT_ROOT="${OUTPUT_ROOT:-$PWD/outputs}"
