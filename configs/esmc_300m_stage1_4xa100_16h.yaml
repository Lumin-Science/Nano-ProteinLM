schema_version: 1
model: esmc_300m
expected_parameter_count: 332997184
seed: 20260822

# Fresh 16-hour Stage-1 campaign. The four-hour production-gated run reached
# 20,971 optimizer steps in 14,400.37 synchronized training seconds, so 84,000
# is the preregistered nominal target and the wall-time cap remains authoritative.
learning_rate: 0.000233695
weight_decay: 0.025675
betas: [0.9, 0.95]
gradient_clip_norm: 1.0
minimum_lr_ratio: 0.1
max_steps: 84000
warmup_steps: 8400

attention_backend: flash
gradient_checkpointing: false
compile: false
compile_mode: default
walltime_seconds: 57600
log_interval: 25
peak_bf16_tflops_per_gpu: 312.0
stages:
  - name: stage1
    context_length: 512
    micro_batch_size: 64
    gradient_accumulation: 1
    mixture: {uniref90: 0.36, mgnify: 0.11, omg_img: 0.54}

evaluation:
  checkpoints: [checkpoint-final.pt]
  profile: full
  pcore_tasks: all_six
  pcore_bootstrap: 10000
  pcore_task_parallel: 6
  pcore_probe_threads: 4
  pcore_batch_residues: 32768
  contact_chains: 20775
  contact_bootstrap: 5000
  contact_shards: 3

paper_reference:
  stage1_steps: 1000000
  stage1_context: 512
  stage1_global_sequences: 8192
  approximate_global_tokens: 4200000
  warmup_steps: 1000
