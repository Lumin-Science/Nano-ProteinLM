schema_version: 1
model: esmc_300m
expected_parameter_count: 332997244
seed: 20260823

# Provisional step budget copied from the incumbent. The mandatory four-GPU
# smoke run will replace max_steps and warmup_steps before the actual run.
learning_rate: 0.000233695
weight_decay: 0.025675
betas: [0.9, 0.95]
gradient_clip_norm: 1.0
minimum_lr_ratio: 0.1
max_steps: 10916
warmup_steps: 1092

attention_backend: flash
gradient_checkpointing: false
learned_residual_routing: true
compile: false
compile_mode: default
walltime_seconds: 7200
log_interval: 10
peak_bf16_tflops_per_gpu: 312.0
stages:
  - name: stage1
    context_length: 512
    micro_batch_size: 64
    gradient_accumulation: 1
    mixture: {uniref90: 0.36, mgnify: 0.11, omg_img: 0.54}
