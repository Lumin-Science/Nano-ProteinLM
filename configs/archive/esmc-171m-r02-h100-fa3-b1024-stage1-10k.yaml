# R02 recipe aligned with the new default's batch, warmup, base weight decay,
# base LR, data mixture, seed, Stage-1 step budget, and H100 FA3 execution settings.
# Shared base LR is 5e-4; the retained Muon group multipliers remain 0.9/0.75.
# Shared base weight decay is 0.01; Muon's retained 0.75 multiplier gives 0.0075.
# Derived from autoresearch_171m_4xl40s_1h.yaml; the historical record is unchanged.
# This aligned configuration has not yet been trained or quality-qualified.
schema_version: 1
model: esmc_171m
expected_parameter_count: 170559856
seed: 20260824
optimizer: muon
muon_momentum: 0.95
muon_lr_scale: 0.8
muon_attention_lr_scale: 0.9
muon_ffn_lr_scale: 0.75
muon_weight_decay_scale: 0.75
muon_nesterov: true
muon_ns_steps: 5
muon_adjust_lr_fn: match_rms_adamw
learning_rate: 0.0005
weight_decay: 0.01
betas: [0.9, 0.95]
gradient_clip_norm: 1.0
warmup_steps: 1000
rotary_base: 20000.0
attention_backend: flash3
gradient_checkpointing: false
learned_residual_routing: true
transformer_norm: rmsnorm
depth_scaled_residual_init: true
compile: false
compile_mode: default
max_steps: 10000
schedule_steps: 10000
# Shared four-hour emergency guard. Compare completed 10k-step runs;
# matching optimizer steps does not imply equal wall time.
walltime_seconds: 14400
log_interval: 10
peak_bf16_tflops_per_gpu: 989.5
stages:
  - name: stage1
    context_length: 512
    micro_batch_size: 64
    gradient_accumulation: 4
    mixture: {uniref90: 0.36, mgnify: 0.11, omg_img: 0.54}
