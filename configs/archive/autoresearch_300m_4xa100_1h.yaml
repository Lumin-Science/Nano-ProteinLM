schema_version: 1
model: esmc_300m
expected_parameter_count: 332823484
seed: 20260824

# Canonical one-hour AutoResearch incumbent. This preset is never selected by
# default; pass CONFIG explicitly to opt in. Wall-clock progress drives the
# final-20% cooldown, and no optimizer-step cap can stop training early.
learning_rate: 0.000233695
weight_decay: 0.025675
betas: [0.9, 0.95]
gradient_clip_norm: 1.0
minimum_lr_ratio: 0.1
warmup_steps: 554
stage1_cooldown_fraction: 0.2

attention_backend: flash
gradient_checkpointing: false
learned_residual_routing: true
transformer_norm: rmsnorm
depth_scaled_residual_init: true
compile: false
compile_mode: default
walltime_seconds: 3600
log_interval: 10
peak_bf16_tflops_per_gpu: 312.0
stages:
  - name: stage1
    context_length: 512
    micro_batch_size: 64
    gradient_accumulation: 1
    mixture: {uniref90: 0.36, mgnify: 0.11, omg_img: 0.54}
