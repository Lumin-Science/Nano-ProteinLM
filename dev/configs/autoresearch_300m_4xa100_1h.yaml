schema_version: 1
model: esmc_300m
expected_parameter_count: 332997184
seed: 20260824

# Seed timing configuration for the one-hour original-ESMC baseline. Replace
# max_steps and warmup_steps from at least 50 steady-state smoke steps before
# the actual run; warmup must remain exactly 10% of the estimated step budget.
learning_rate: 0.000233695
weight_decay: 0.025675
betas: [0.9, 0.95]
gradient_clip_norm: 1.0
minimum_lr_ratio: 0.1
max_steps: 180
warmup_steps: 18

attention_backend: flash
gradient_checkpointing: false
compile: false
compile_mode: default
walltime_seconds: 120
log_interval: 10
peak_bf16_tflops_per_gpu: 312.0
stages:
  - name: stage1
    context_length: 512
    micro_batch_size: 64
    gradient_accumulation: 1
    mixture: {uniref90: 0.36, mgnify: 0.11, omg_img: 0.54}
