schema_version: 1
model: esmc_171m
expected_parameter_count: 170559856
seed: 20260824

# Validated ESMC-171M AutoResearch baseline. Three-seed validation retained the
# differential Muon learning rates and RoPE base 20,000 from campaign R02.
optimizer: muon
muon_momentum: 0.95
muon_lr_scale: 0.8
muon_attention_lr_scale: 0.9
muon_ffn_lr_scale: 0.75
muon_weight_decay_scale: 0.75
muon_nesterov: true
muon_ns_steps: 5
muon_adjust_lr_fn: match_rms_adamw
learning_rate: 0.000326599
weight_decay: 0.0183712
betas: [0.9, 0.95]
gradient_clip_norm: 1.0
warmup_steps: 554

rotary_base: 20000.0
attention_backend: flash
gradient_checkpointing: false
learned_residual_routing: true
transformer_norm: rmsnorm
depth_scaled_residual_init: true
compile: false
compile_mode: default
walltime_seconds: 3600
log_interval: 10
peak_bf16_tflops_per_gpu: 312.0
stages:
  - name: stage1
    context_length: 512
    micro_batch_size: 64
    gradient_accumulation: 1
    mixture: {uniref90: 0.36, mgnify: 0.11, omg_img: 0.54}
